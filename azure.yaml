name: python-agent-tutorial
metadata:
  template: local
services:
  web:
    project: ./src
    language: python
    host: appservice
hooks:
  postprovision:
    run: ./.azure/hooks/postprovision.sh
    shell: sh